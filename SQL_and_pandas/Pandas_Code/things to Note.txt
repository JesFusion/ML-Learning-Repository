{
    RULE:
    - Use pd.merge() when joining on COLUMNS.
    - Use df.join() when joining on INDICES.
},

{
    RULE:
    - loc uses index to locate rows
    - iloc uses number to locate rows
},


{
    Rule: NEVER use f-strings or + to put user variables in a SQL query. ALWAYS use "params = "
},

{
    How to use "if_exists" parameter in pd.read_sql():
    
    if_exists: What to do if the table already exists?

    - 'fail' (Default): Stop and give an error. Safe, but annoying.

    - 'replace': Drop the old table and create a new one. Good for "refreshing" a daily report.

    - 'append': Add your new rows to the bottom of the old table. Good for "logging" data over time.
},

{
    nominal Data: Data that has no order/rank (e.g: East, West, North, South)

    ordinal Data: Data that has order/rank (e.g: Big, Medium, Small)
},

{
    the .replace() method is used to remove unwanted characters from object datatypes in python. For Example:
    "To remove the "$" and "," symbol from the values in a prices column (and convert to numbers), use:"

    df['prices'] = df['prices'].replace('[\$,]', '', regex=True).astype(float)

    Explanation:

    1. `df['prices']`  
    Accesses the `prices` column in your DataFrame.

    2. `.replace('[\,]', ”, regex=True)`  
    - This uses a regular expression (regex) to remove unwanted characters.
    - `[$,]` means: match any `` or `,`.
        - `$` → matches a literal dollar sign (``)
        - `,` → matches a comma
    - `”` → replaces the matched characters with nothing (i.e. removes them)
    - `regex=True` tells Pandas to treat the pattern as a regex.

    3. `.astype(float)`  
    Converts the cleaned string values into actual floating-point numbers.
},

{
    tablefmt is a parameter in .to_markdown() that enables you to customize table styles. various options are:
    
    Basic: plain, simple, github, pipe, orgtbl, jira, presto, pretty, psql, rst, mediawiki, moinmoin, youtrack, textile, tsv
    ==> df.to_markdown(tablefmt="github")
    
    
    Grid: grid, simple_grid, rounded_grid, heavy_grid, mixed_grid, double_grid, fancy_grid
    ==> df.to_markdown(tablefmt="fancy_grid")
    
    Outline: outline, simple_outline, rounded_outline, heavy_outline, mixed_outline, double_outline, fancy_outline
    ==> df.to_markdown(tablefmt="mixed_outline")
    
    Code/Markup: html, unsafehtml, latex, latex_raw, latex_booktabs, latex_longtable, asciidoc 
    ==> df.to_markdown(tablefmt="latex_booktabs")
}

